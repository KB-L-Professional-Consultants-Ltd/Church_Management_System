{
  "name": "church_management_system",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "prepare": "husky"
  },
  "lint-staged": {
    "apps/nextjs/**/*.{ts,tsx,js,jsx}": [
      "pnpm exec eslint --max-warnings=0 --config apps/nextjs/eslint.config.mjs",
      "prettier --write"
    ],
    "apps/api/**/*.{ts,js}": [
      "pnpm exec eslint --max-warnings=0 --config apps/api/eslint.config.mjs"
    ],
    "packages/ui/**/*.{ts,tsx,js,jsx}": [
      "pnpm exec eslint --max-warnings=0 --config packages/ui/eslint.config.ts",
      "prettier --write"
    ],
    "**/*.{json,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.2",
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "turbo": "^2.8.17",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  }
}
